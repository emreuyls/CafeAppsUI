import { TextOverFlowPipe } from './text-over-flow.pipe';

describe('TextOverFlowPipe', () => {
  it('create an instance', () => {
    const pipe = new TextOverFlowPipe();
    expect(pipe).toBeTruthy();
  });
});
